# Phase 5 -- Budget Estimation & Cost Optimization

## Objective

Implement cost estimation logic.

## Requirements

Calculate: Hotel + Travel + Meals

Formula: (HOTEL_PER_DAY × DAYS) + TRAVEL + (MEAL_PER_DAY × DAYS)

Display summary table with: - Item - Cost (PKR)

## Antigravity Prompt

Create budget estimation service with configurable rates stored in JSON.

## Rules

-   No hardcoded totals
-   Must calculate dynamically
-   Return breakdown as JSON
